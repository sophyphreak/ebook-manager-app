import releaseToNodemailer from '../../../../containers/ReleaseSubmitter/sendReleaseToNodemailer/sendReleaseToNodemailer';

test('Should import releaseToNodemailer correctly', () => {
  expect(1).toBe(1);
});
