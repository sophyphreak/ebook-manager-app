import releaseToNodemailer from '../../../../containers/PressReleaseSubmitter/sendReleaseToNodemailer/sendReleaseToNodemailer';

test('Should import releaseToNodemailer correctly', () => {
  expect(1).toBe(1);
});